#include <subject.hpp>
#include <student.hpp>
#ifndef GATHER_HPP
#define GATHER_HPP
std::vector<STUDENT> gather_students (std::vector<SUBJECT *> initialized_subject_list);
std::vector<SUBJECT> gather_subjects (void);
#endif
